# Agent Chat UI

This directory hosts the Agent Chat UI created via `create-agent-chat-app`.

Use `./scripts/sync_ui_config.sh` to write defaults from `ami/langgraph.json`
into `web/.env.local` (base URL defaults to `http://127.0.0.1:2024`).
