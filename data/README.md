# Markdown Data Store

This folder holds local Markdown files used by the agent.

Suggested layout:
- `data/todos/` daily and weekly todo lists
- `data/schedules/` calendar snapshots and plans
- `data/reports/` weekly reports generated by the agent
- `data/news/` topic digests per day
